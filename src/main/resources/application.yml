server:

spring:
  batch:
    initialize-schema: never
    job:
      enabled: true
  thymeleaf:
    cache: false

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/quickflash?characterEncoding=UTF-8
    username: root
    password: root

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

    properties:
      hibernate:
        format_sql: false

mybatis:
  mapper-locations: mappers/*Mapper.xml

 
strava:
  client-id: 166853
  client-secret: c1786d31435c9fbcbde3f4a6e094a5ef7b1cefb9
  redirect-uri: http://localhost:8080/oauth/strava/callback
