<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quickflash.comment.mapper.CommentMapper">

    <update id="updateMileage" parameterType="com.quickflash.mileage.domain.Mileage">
        UPDATE mileage
        SET mileageOfCycle = #{mileageOfCycle},
        mileageOfRunning = #{mileageOfRunning}
        WHERE userId = #{userId}
        AND createdAt = #{createdAt}
    </update>

    <insert id="insertMileage" parameterType="com.quickflash.mileage.domain.Mileage">
        INSERT INTO mileage (userId, mileageOfCycle, mileageOfRunning, createdAt)
        VALUES (#{userId}, #{mileageOfCycle}, #{mileageOfRunning}, #{createdAt})
    </insert>
    <select id="selectMileageByUserIdAndCreatedAt" parameterType="map" resultType="com.quickflash.mileage.domain.Mileage">
        SELECT
        userId AS userId,
        mileageOfCycle AS mileageOfCycle,
        mileageOfRunning AS mileageOfRunning,
        createdAt AS createdAt
        FROM mileage
        WHERE userId = #{userId}
        AND createdAt = #{createdAt}
    </select>


</mapper>